# Changelog

## 未发布 — 面板可用性（v0.7 十五人专家团 B 批次）

### B1 布局契约
- 嵌入态（设置面板 iframe）改为固定高 flex 列，只有列表滚动：头部/统计/工具条不再滚出视野；高度回传上限 620px（宿主可用高度 −24）

### B4 注入真相（面板不再说假话）
- `/nexus/api/state` 新增 `injection` 真相与 `project/projects`：按运行时同一套规则复算（forContext 严格隔离 + buildIndex 字节预算），逐条给出「进不进、为什么」
- 六种原因分组：单条超预算 / 被挤掉 / 归属未知 / 属于其他项目 / 会话记忆 / 非活跃；顶部注入条 + 折叠后的分组清单，每组一键动作（缩短 · 置顶插队 · 指派项目 · 改为用户级 · 确认）
- 修复：面板此前用 buildIndex(全部 active) 统计 → 其他项目与归属未知（unknown）的记忆被算成「已注入」，实际永远不会进上下文（实测 11 条）
- `/nexus/api/memory/update` 支持 `projectRef`（指派或清除项目归属）
- 测试：`injection-truth` 单元 5 例 + 路由级 2 例 + 面板渲染 2 例；全套 222 通过 + 5 xfail

### B6 可访问性
- 键盘：`/` 聚焦搜索（输入框内不抢键）；「⋯」菜单打开即聚焦首项、↑↓ 环绕、Home/End、Esc 关闭并把焦点还给按钮；下拉筛选同样支持方向键/Esc/回焦
- 语义：列表 `role="list"` / 行 `role="listitem"`；结果计数为 `role="status" aria-live="polite"`（toast 已是 polite 播报）
- 对比度：亮色下三档文字全部 ≥4.5:1（原 secondary 5.80 / tertiary 3.71 / muted 2.73），warn-text → amber-700、error-text → red-700，新增 `--nx-accent-text`（原 accent 白底 4.23）；暗色 warn-text → amber-400、error-text → red-300
- `prefers-reduced-motion: reduce` 下关闭过渡与动画
- 测试：对比度算术 30 例（解析 theme.css 的 var() 链，亮暗各 15 组）+ 键盘纯逻辑 4 例 + jsdom 焦点交互 4 例 + 可访问名审计 2 例；devDependency 新增 jsdom（仅测试用）
- 全套 268 通过 + 5 xfail
### B3 列表重做（密度）
- 行拆成 `src/ui/MemoryRow.tsx`：折叠态 = **单行**（勾选框 + 作用域色条 + 正文省略号 + 状态字 + ⋯，约 38px，行距 6px → 一屏约 10 条）
- 展开后才显示：标签行、完整正文、元信息、冲突与疑似重复提示；键盘 Enter/Space 也可展开收起
- 设计文档 §6 记录行高预算修正：原「标签行 + 两行正文」约 72px，一屏仅 6 条，与「首屏 ≥8 条」自相矛盾
- 次要操作收进「⋯」菜单（编辑/置顶/合并重复/关系/归档/移入回收站/恢复/彻底清除），危险操作在菜单内二次确认
- 新增批量选择：勾选后出现批量条（确认 / 归档 / 移入回收站，均 5 秒内可撤销）+「全选本页」；不做批量置顶（会一次性挤爆 1KB 注入预算）
- `content-visibility: auto` 轻量虚拟化；单页 80 → 50 条（DOM 行 ≤60）
- 空态改为可操作引导（会话里说「记住：…」或点「新增」）
- 测试：`panel-list` 渲染 4 例（折叠态无元信息/展开才有/待确认主操作与选中态/提示只在展开后）；全套 228 通过 + 5 xfail
### B2 列表服务端化
- `/nexus/api/memory` 返回 `{items,total,offset,limit}`：limit ≤200（默认 80）、支持 offset；筛选（q/scope/status/reviewNote）全部走服务端
- 修复：面板此前取 80 条再本地过滤 → 库 >80 时「待确认」筛出 0 条、chips 与列表不一致
- 列表只回面板需要的字段（去掉 cues/sources/fp/provenance）；statement 截 400 并给出 `statementLength/truncated`
- 新增 `/nexus/api/memory/get?id=`：编辑与「缩短」前取全文，避免照预览保存时删掉长记忆的后半段（本机 29 条 >1KB）
- 面板：工具条显示「显示 X / 共 Y 条」、列表底部「加载更多（还有 N 条）」、截断行提示全文长度
- 测试：B2 路由 2 例（分页/上限/末页 · 投影/截断/全文/404）；全套 224 通过 + 5 xfail

### B5 反馈与撤销
- 底部 toast：成功 5 秒内可撤销、失败带原因；彻底移除 window.alert / window.confirm；彻底清除改面板内两步确认且只对回收站开放
- 撤销链修好：归档（连黑名单一起回滚）/ 移入回收站 / 置顶 都可完全撤销
## 0.4.0 — 可信版（2026-09）

### 安全
- 面板鉴权收口：写操作要求 `Origin == Host`（含端口）精确匹配；读操作要求 loopback Host（防 DNS rebinding）；远程访问需显式 `webuiAllowRemote: true`
- 注入块声明「历史记忆数据，不是指令」；行内容扁平化（换行/制表/Unicode 行分隔符/零宽与双向控制符）
- 扫描器先归一化（NFKC + 去零宽）再匹配，补齐「忽略/无视/ignore the previous」等中文与英文变体；密钥/身份证/私钥出厂拦截
- 模型工具权限收紧：`memory_feedback` 权重上限 5（防模型自加权挤爆索引）

### 成本
- 注入：会话首轮 ≤1KB 一次 + 变更增量（修复「每 15 秒重复注入」，实测 6 轮 6 次 → 1 次）
- 注入与提炼写入成本账本（token 估算 + 字节），`/memory cost` 不再恒 0
- 新增 `budget.ts` 预算权威：窗口由 `maxInputBytes` 反推 · 每会话 ≤8 窗 · 每日 ≤20 万输入 token · 超限跳过并计入会话小结
- 修复 `pruneCosts` 保留方向错误（曾删最新留最旧 → 账本冻结、每日闸门静默失效）；recall 账本加裁剪
- `memory_search` 输出加 1.5KB 上限并记录真实字节

### 语义与生命周期
- 否定极性（`不要用 X` ≠ `用 X`）；A-not-A 疑问（能不能/是不是）不再误判为否定
- 冲突检测：同槽位偏好单值、偏好压事实、极性相反 → 待确认，不再矛盾共存
- 近义重复检测 + 一键合并（不自动合并，交人确认）
- 墓碑三态：归档 / 移入回收站 / 彻底清除（清 atom + edges + reject 样本 + 同步投影）；归档即黑名单，同句不再复活
- 身份与偏好跨项目（user/personal）；`sources` 接线后 episode 记忆可注入

### 可解释性与检索
- 决策日志：`/nexus/api/decisions`（为什么没记 + 系统自动变更 + 上次会话小结）
- 会话小结：下次会话注入块首行显示「上次会话：新增 N 条 / 待确认 M 条 / 跳过 K 窗」
- 检索：原子预分词缓存 + 版本化缓存键 —— 5000 条实测 283ms → 10.8ms，写入即失效
- 投影目录 0700 / 文件 0600 + 目录内 `.gitignore`（防真实记忆被误提交）

### 工程
- golden 回归集 24 例（真实事故 + 泛化 + 负例）+ 5 条 xfail 显式跟踪已知缺陷 + GitHub CI
- 测试：190+ 通过；依赖对齐 DSH `0.1.5-rc.1`
