# Changelog

## 0.8.13 — 反死机制清零 + 向量检索补测（2026-09）

### 反死机制：三个违反项全部收口
- **`linkCluster` 接线**：integrator 在非 dry-run 下给聚类成员两两建 `semantic` 共现边（幂等，n 个成员 n(n-1)/2 条），`/memory integrate run` 的输出补「建 semantic 边 N 条」。此前它全仓零调用点 —— integrator 只写概况记忆、从不建边，于是面板「相关邻里」（`neighborsOf`）永远看不到聚类关系
- **`sessionToPanelMode` 收敛**：面板按钮值 ↔ 内部会话模式的映射曾在**三处各写一遍**（scheduler / web-ui 的 `/nexus/api/mode` / 面板 BPanel），类型还重复声明了两处（`config.ts` 与 `scheduler.ts`）。全部收敛到新增的 `src/modes.ts`（无依赖，因此浏览器 bundle 也能直接 import；`scheduler.ts` 会拖进 cordis，不行），并补了 `isPanelMode` 做入口校验
- **机器清单上线**：状态表新增「零调用点的导出」小节（`deadExports()` 现算），与「零测试引用的模块」并列 —— 从此「有实现、没接线」不再靠人记。当前只剩一个测试辅助函数（`resetExtractionBudgetForTests`）
- 新增 `tests/edges.test.ts`（6 例：n(n-1)/2 条边、幂等与顺序无关、不建自环、dry-run 不落地、run 必须建边）与 `tests/modes.test.ts`（4 例：映射不串台、往返一致、枚举完整、入口校验）

### 向量检索：补测试，并因此抓出一个真缺陷
- 新增 `tests/retriever-vector.test.ts`（9 例：降级契约 / RRF 名次融合 / 余弦边界 / 编码全挂回退纯文本 / 空结果与过短 query 不打网络）
- **缺陷**：`HttpEncoder.encode` 抛错时不自增失败计数，「连续 3 次失败降级」实际由调用方在 catch 里代记 —— 单独用 `encode` 的那条路永远不会降级。改成编码器自己计数，调用方不再补记（否则一次失败会被数两遍、两次就降级）
- 仍未上线：默认关闭不变（不传 / 传 false / 传 true 都得到 false）。下一步是影子对比（关键词 Top-K vs +向量 Top-K），但那需要先有一个可用的 embedding 端点：宿主 DSH 没有 embeddings seam（checkout 里搜不到），而本机 provider 中的 zai（智谱）有 `embedding-3`

### 零测试引用的模块 10 → 6
`retriever-vector` / `processors` / `edges` / `integrator` 因新测试脱离名单；仍剩 `degrade` / `events` / `extractor-llm` / `importer` / `skill-compiler` / `ui/index.tsx`

## 0.8.12 — 文档一致性收口 + 影子计数覆盖全部写入路径（2026-09）

### 写入
- **价值门影子计数的覆盖面**：调用点从 scheduler 的**触发词分支**移进 `Facility.saveAtom`（`recordValueShadow`，在 `buildAtom` 之后、gate/forgetter 分支之前）—— 触发词、工具失败、goal/todo、`/memory` 命令、面板新增、导入、集成器全都记到，每条候选只记一次（重复内容在 fp 去重那步就返回），被安全扫描/黑名单拦下的候选不计。回归背景：切换条件写的是「连续 7 天 / reject ≥50 条 / 误拦率 0」，而样本只从一条路径来，那两个阈值**根本测不到**；新增 3 条回归测试（任一路径都记、fp 重复不重复计数、被扫描拦下的不记）。行为未变：仍然只记判定、不拦截

### 文档与门禁（`npm run docs:check` 从 1 张网变 3 张）
- **叙述门禁**：注册表 `_removed_symbols` 登记一批已删除符号（`?panel=old`、`coldArchive`、episode TTL、`memory_trace`、`dsh-inject-scheduler`…），活文档（README / NEXUS-DESIGN / DOC-MAINTENANCE / V0.5-DESIGN）再提到它们时**同一行必须带豁免词**，否则失败并点名行号 —— 探针只查代码事实，查不到「README 说包内有三套面板实现」这种叙述漂移
- **零测试引用的模块现算**：README 曾写死「另有 8 个运行时模块零测试引用」，增删模块就又过期；现在由 `untestedModules()` 现算并写进状态表独立小节，README 只指向它
- **修掉 4 处已确认的漂移**：README 两处过期 ⚠️（「包内打包了三套面板实现」、「`runLifecycle` 与 `linkCluster` 是导出即死代码」）、`docs/status.json` 的 `panel-default-b` 自相矛盾（同一条注册表里说旧面板「三份并存」）、`NEXUS-DESIGN` 的冷迁移条目与宿主 seam 示例、`DOC-MAINTENANCE` 的五条历史坑（逐条标了结局）
- **`CHANGELOG.md` 从 0.4.1 补齐到 0.8.11**：28 个版本条目；0.8.9–0.8.11 无 git 提交，靠逐版 tarball 解包 diff 核实（`coldArchive` / `tickLifecycle` / `scannerRules` / 问句正则 / `nx-select` 的出现与消失）
- **`V0.5-DESIGN.md` 纳入叙述门禁**：它以版本命名但正文一直挂着 `> 实现状态` 复核标注，属于活文档；D4/D6/§一 的标注按当前实现复核（7 天清除撤销、`memory_feedback` 与衰减已修、套件数 41/400+ → 34/398）

### 决定收口（4 项 defer 的其中 3 项）
- **价值门**：先补影子覆盖面再攒样本，**不改拦截行为**（回放证据：真实库 34 条 → accept 0 / review 3 / reject 31，误拦 0，但样本以历史垃圾为主、观察期不足）
- **integrator + `skill-compile`**：**保留**，明确标注为「记忆 → 技能」的待用方向 —— 删代码可逆（git），放弃方向不可逆；顺带补登记 `cmd-skill-compile`（此前它根本没进注册表）
- **回收站 7 天自动清除**：🗑️ **撤销承诺**（时间驱动的自动清除不可逆，与「删除可回溯」冲突；面板二次确认已够），注册表状态 `missing` → `removed`，不再当欠办
- 向量检索 + RRF 仍在待决定（见下个版本）

## 0.8.11 — 三处下拉换成自绘控件（2026-09）

### 面板
- **原生 `<select>` 整个换掉**：状态筛选 / 作用域筛选 / 提炼模型三处改用新增的 `src/ui/Select.tsx`（`button` + 自绘列表，`BPanel.tsx` 三处引用）。用户两次反馈「下拉形式不好看 / 和整体风格不搭配」，而**露馅的是弹层**：原生下拉的弹层是系统菜单（macOS 亮色模式下就是一张白底 NSMenu），CSS 够不着，`color-scheme: dark` 也只在部分平台生效 —— 0.8.10 的 `appearance: none` + 自绘雪佛龙只统一了收起态，点开仍是白菜单
- **顺带修掉宽度不齐**：原生 select 的宽度由最长选项决定，同样 padding 下雪佛龙与文字的间距时宽时窄（「全部状态」空一截、「全部作用域」几乎贴住），并排两个控件不像一套；自绘后触发器与 `--bg-input` / `--border` / `--radius` 同一套语言
- **弹层定位**：`position: fixed` 逃出 `.panel-body` 的滚动裁剪；下方放不下整张列表且上方更宽裕时翻到触发器上方，越出右边缘时右边缘对齐触发器；限高 280px（状态筛选 8 项一次看全）；键盘 ↑↓ Home End Enter Space Esc Tab，角色 `combobox` / `listbox` / `option`
- 新增实测工具 `scripts/panel-select-shot.mjs`（`npm run panel:select`）：把本地构建塞进 418×620 的 iframe，量触发器与**展开后弹层**的盒模型、配色与几何（gap 正数=在触发器下方，负数=在上方）并截图 —— 下拉好不好看只有真的渲染出来才看得见
- 状态表新增条目 `panel-custom-select`（覆盖状态 / 作用域 / 提炼模型三处）；README 补「下拉自绘」一节

## 0.8.10 — 下拉的第一次修复（没修对）（2026-09）

### 面板
- 先按 CSS 修：`.lib-select { appearance: none }` + 自绘雪佛龙（颜色取 `--text-3`），去掉深色面板里露出的系统 ⇅ 箭头；弹层交给 `color-scheme: dark` 跟随深色（0.8.10 包内 `lib/nexus.html` 的 CSS 注释原文）
- **结论：没修对**。露馅的是弹层，而它是系统菜单 —— 只看 CSS 看不出来；用户复看后仍反馈不搭，0.8.11 才整个换掉。这条教训写进了 `scripts/panel-select-shot.mjs` 的文件头：改下拉请务必真渲染跑一遍

### 写入
- 触发词表补**繁体写法**（我的習慣是 / 請記住 / 別忘記 / 以後一直…），`QUESTION_RE` 补「还是…吧」句尾 —— 两处只出现在 0.8.10 及以后的包里，0.8.9 包内还没有

## 0.8.9 — 记忆层修复批次 + 面板收敛为单一实现（2026-09）

### 让「有实现、没接线」的机制真的跑起来
- **`runLifecycle` 接线**：写入路径（`saveAtom` / `review`）驱动 `tickLifecycle`，模块内 6 小时节流、无后台定时器、tick 出错 fail-open —— 此前它全仓零调用点，权重衰减从未执行过；同批 `scannerRules: 'minimal' | 'recommended'` 进 schema 并真的被 `createScanner` 读取（`RECOMMENDED_RULES` 不再是运行时不可达的死规则集）
- **硬拒绝补齐 + 问句判定修正**：临时话题（只认显式当下性措辞，保守判定）与代码可推导（自述来源是仓库文件）两条规则真正返回，`rulesText` 由 scheduler 从 cwd 的 AGENTS.md / CLAUDE.md 读取传入；`QUESTION_RE` 原为 `[吗呢么]`，于是**任何以「么」结尾的陈述**（如「提交信息要用中文说明改了什么」）都被判成问句 —— 改为 `[吗呢]` 后长期 xfail 的 xf-03 转正
- **第三方转述拒收 + 拒收全留痕**：`THIRD_PERSON_QUOTE_RE`（他/她/别人/同事… + 说/认为/要求）拦下「他说，记住要用 pnpm」这类别人的话；RejectLog 覆盖扫描器拒收与门控拒收（此前只有触发词硬拒与用户拒绝两条路径写日志）
- **删掉真·死代码**（类型、schema、ResolvedConfig 与文档一并移除；旧配置带着未知键仍能加载）：`mode: strict|standard|loose` 三模式（零行为分支，面板/会话的三态模式不受影响）、`coldArchive`、episode 90 天 TTL 归档（按时间归档 active，与「已确认 active 永不受时间影响」冲突，回归测试断言 900 天前的 episode 仍是 active）、`status:'deleted'` + `deletedAt`、`memory_trace` 工具、`dsh-inject-scheduler` 回退、`edges` 空旋钮、索引 ≤200 行截断、投影脏位重试、`bench/golden.jsonl` 112 条、CI 的 L0/L1 两层、5 条无数据源的指标线

### 工具与命令
- **`memory_read` 默认只读 active**：非 active 返回「默认不可读，要回溯归档链请加 `trace: true`」；`trace` 沿 `supersedes` / `supersededBy` 双向走完整 lineage（有环与长度保护）
- **`memory_feedback` 的 `bad` 真的降权**：`good` 加权并刷新 `updatedAt`（视作一次「被用到」）；`bad` 只降权（下限 1）且**不刷新**时间 —— 刷新会让它看起来更新鲜，反而在注入排序里往前挤，与反馈意图相反；权重夹在 1..20
- **`/memory` 补齐命令（到 15 个）**：新增 `edit`（重算指纹与线索、撞指纹则拒绝保存）与 `delete`（走回收站路径：归档 + `user-deleted`，与面板同一条）；`reject` 按 `nex_` 前缀区分 id 与原因（不再把原因当 id 吞，也不再恒为 `'user rejected via command'`）；`conflict` 支持 `keep-new` / `keep-old` / `keep-both` 一键三选 + `--all` 批量

### 面板与工程
- **面板收敛为单一实现**：0.6 的 `NexusPanel` 与样例复刻档 `SamplePanel`（连同 InjectionBar / MemoryRow / RefLayout / ScopeBar 等专属依赖与 7 个测试文件）一并删除，`?panel=old` / `?panel=sample` 路由移除，theme.css 里 145 个旧面板类清空（43 KB → 8.7 KB）；面板 HTML 348 KB → 223 KB、bundle 254 KB → 186 KB
- **噪声扫描覆盖非活跃条目**：`collectNoise` 此前只收 `active`，而垃圾常常正躺在 pending / needs-review 里 —— 实测本机 34 条库中 29 条是垃圾（27 条已归档、2 条卡在 needs-review），于是横幅计数为 0、一键清理入口不出现，用户反而在归因页看到邀请他点「确认」的垃圾卡片；现扫所有非 archived/superseded，与 `/memory doctor` 同口径；同批 `storeWritable` 改走真实写探测（把当前状态原样写回，不产生脏数据），「✖ 不可写」结论才可达
- **文档状态表上线**：`docs/status.json`（唯一事实源）+ 生成 `docs/IMPLEMENTATION-STATUS.md`（能力状态表：已实现 / 部分实现 / 未实现 / 承诺已撤销 四类，逐条带代码探针与「打算怎么办」的处置）+ `docs/DOC-MAINTENANCE.md` 规约；`npm run docs:check` 用代码探针校验「声明的能力在代码里有没有、接没接线」，接入 CI 与 pre-commit —— 上线即抓到 README「已知限制」还留着「包内打包了三套面板实现」这类叙述漂移

## 0.8.8 — 表面类名补齐 + 展开态吃满宿主高度，新增真浏览器调试（2026-09）

### 面板
- **真机调试抓出两个预览壳看不见的 bug**：① 根节点从来没有 `.panel-open` 类 —— 0.8.6 写好的「统一表面」CSS（背景 / 圆角 / 边框）其实从未生效（预览壳只量面板内部元素，看不出「该有而没生效」）；② 展开时 iframe 只报 471px，而设置弹窗有 800px —— 预览壳写死 iframe 尺寸，`desiredHeight()` 的上报值被忽略
- 两处修复：根节点 className 补 `panel-open`；面板打开时 `desiredHeight()` 直接返回宿主可用高（不再按「内容最多 6 张卡」算），iframe 上限 1000 → 2000
- 新增 `npm run debug:web`（`scripts/debug-web.sh`，默认端口 3199）：用 workspace 内的 `DSH_HOME` 与用户实例完全隔离、把最新 tarball 热替换进调试 profile，配 `scripts/debug-real.mjs` 走 CDP 程序化量几何并截图 —— 真机实测（设置弹窗 800×800）：展开 564×764，面板底 = 内容区底 = iframe 底，与「Agent 预设」同高

## 0.8.7 — 面板铺满实测 + 保留最外圈边框（2026-09）

### 面板
- 用三段 flex 列实测证明 `panel-body` 到底了：53（模式 + 预算行）+ 41（标签行）+ 526（内容区）= 620 = iframe 高，底部没有空余；此前「看起来没到底」是两层表面露出的底色造成的错觉
- 最外圈的边框**保留**，且落在根节点 `.nx-b.panel-open` 上：`border: 1px solid var(--border-frame)`（#33333A，不透明）、圆角 8px、阴影 —— 圆角 + 边框 + 阴影都在最外那一圈，面板内容不再有第二层边框

## 0.8.6 — 面板与 iframe 合成同一张表面（2026-09）

### 面板
- 根因是两层表面：遮罩（半透明黑底 + 8px 内边距 + 圆角）+ 里面的面板卡片（深色底 + 圆角 + 阴影）→ 必然出现外圈暗边 + 内层直角的分裂感
- 改法：面板打开时给根节点 `.nx-b` 加 `.panel-open`（背景 + 圆角 + 边框 + 阴影，它自己就是那张卡），`.overlay` 变成完全透明的点击层（只负责点外部关闭与铺满可用高），`.panel` 背景透明、无边框无圆角；新增不透明 `--border-frame`
- 实测 iframe 620 / 900 时根节点 = 遮罩 = 面板 = 620×418 / 900×418，内容区 526 / 806，文档溢出 0

## 0.8.5 — 面板高度对齐 Agent 预设（2026-09）

### 面板
- 用户对比「Agent 预设」页发现记忆面板下面留空：原因是面板自身的 `92vh` 二次压缩（iframe 620 时 92vh = 570，底部稳定空 50px）
- 可用高本身也报小了：原来发父容器 `clientHeight`（面板一长高父容器也长高 → 自反馈抖动）再减 24 → 改 `Math.max(父可视高, 视口高-220, 320) - 16`；iframe 上限 620 → 1000（宿主仍按自己的可用高裁剪）
- 实测（iframe 418×620）：面板高 570 → **604**、内容区 474 → **508**，文档溢出 0

## 0.8.4 — 圆角与状态条同档、宽度对齐（2026-09）

### 面板
- 用户指出两点，两条都成立（都是 0.8.3 改宽度时改过头 / 没到位）：面板不圆角、高度不是 100%
- 修法：圆角回到 `var(--radius)`（8px，与状态条同档）并恢复边框阴影；根节点加同档 8px 内边距，面板与状态条宽度都变成 402/418；`.panel` 加 `height:100%`、遮罩改 `align-items:stretch` 填满可用高
- 实测（iframe 418×620）：状态条 402×43、面板 402×570、遮罩 418×620；门禁 39 文件 / 373 通过

## 0.8.3 — 面板铺满 iframe + 宽屏双栏（2026-09）

### 面板
- 实测定位「状态条铺满、面板窄一圈」：状态条没有宽度上限（418 / 600 / 1000 跟着根走），面板却照抄原型的 `max-width:420px` 再加遮罩 8px 内边距（402 / 420 / 420）
- 改法：`.panel` 改 `max-width:none` 铺满 iframe、`.overlay` 内边距 8px → 0，圆角 / 阴影 / 边框去掉（面板就是这一列本身，不需要卡片感）
- 宽屏（≥560px）改双栏：左列表 + 右详情（sticky，实测 900 宽下约 46% / 54%，窄栏仍单列 + 行内展开）；门禁 39 文件 / 372 通过

## 0.8.2 — 高度自协商支持面板 B（2026-09）

### 面板
- 高度此前确实浪费：自协商只认旧面板选择器（`.nx-app` / `.nx-list`），面板 B 里这两个类一个都没有 → 期望高度恒为 168px，宿主只好用 `minHeight:480` 兜底（收起态只有一条 43px 状态条也占满 480）
- 修法：`desiredHeight()` 增加面板 B 分支 —— 收起态报「状态条高 + 8」（实测 **51px**），展开态报「chrome + min(内容高, 6 张卡)」、上限 620；iframe `minHeight` 480 → 48，宿主把可用高写进 `--nx-b-available`
- 宽度不是能改的：DSH 设置弹窗内容区固定 418px（面板已 `width:100%` 铺满），要更宽只能走独立页 `/nexus`

## 0.8.1 — 面板 B 按四路专家评审修复（2026-09）

### 面板
- 四路专家（交互 HCI / 无障碍 / 窄屏约束）基于源码 + 418×448 实测 + 实拍图评审，修掉四个真 bug：① 遮罩层规则在移植时整段丢失 —— 面板根本不是浮层（无遮罩、无 z-index、「点面板外关闭」是死代码）② 菜单与二级弹窗的 portal 落在样式作用域之外（实测 `position:static`、`rect y=448` 在视口外，编辑 / 置顶 / 归档 / 复制 ID 一个都点不到）
- ③ 归因分组不互斥：同一条记忆同时出现在「进入上下文」与「模型推断」，字节被重复计（两组都 603 B，预算 1024 B）→ 改成互斥三组 ④ 首次加载失败显示「0 条 / 0 B」假状态 → 改成加载中 / 读取失败可重试，不渲染任何数字
- 按专家建议做减法：chrome 230px（占 448 的 51%）→ **95px**（21%），内容区可视高 181/356 → **316/474**，卡片平均高 128px → **69px**（标题 3 行 clamp）
- 对比度与可达性：元信息文字 2.71–2.90:1 → **5.1–5.4:1**，`--text-3` 改 #9096A2、新增按钮底色改 #2563EB；面板加 `role=dialog aria-modal`、标签页 tablist/tab + `aria-selected`、9 个同名「⋯」补 `aria-label`；门禁 39 文件 / 372 通过

## 0.8.0 — 面板 B 移植为默认（2026-09）

### 面板
- 用户给了两版 HTML 并问「如果是你你会猜用那个作为面板呢？」→ 选 B，用户「开始」（docs/V0.8-PANEL-B.md:3）：设置里的「记忆」与 `/nexus` 都换成面板 B
- 结构照搬 B：顶部状态条（「● N 条进入上下文 | X B / Y B | N 条待确认」，点开面板）→ 标题 + 模式三态（记录中 / 只看不记 / 已关闭）+ 预算条 → 统计行（今日写入 / 待确认 / 已拒收 / 注入次数）→ 四标签（归因 · 记忆库 · 回收站 · 设置）
- **归因是 B 的核心**：按「进入上下文 / 置顶插队 / 用户显式声明 / 模型推断 / 未进入 · <原因>」分组，每组给条数与字节；卡片显示标题 · 字节 · 置信度点 · 来源 · 作用域 · 置顶标，未进入的显示原因；误写记忆横幅带「查看 / 一键归档」
- 记忆库：搜索（命中高亮）· 状态筛选 · 作用域筛选 · 新增 · 批量栏（确认 / 归档 / 入回收站）· 展开详情；回收站：单条恢复 + 彻底清空；设置：置信阈值 + LLM 提炼器 + 运行状态
- **为支撑 B 新增的后端能力（都落盘、重启仍在）**：`POST /nexus/api/mode`（readwrite / readonly / paused，写 store 的 `panelMode` 并立即改调度器全局模式，不需要重启）、`/state.mode`、`/state.stats`、`/state.sourceCounts`、`/memory.items[].provenance`
- 与 B 原型的三处落地差异：宽度写死 420px → `100% + max-width`（DSH 设置弹窗内容区实测 418px）；入口不做两级（设置项本身就是入口）；数据全部接真（原型里是写死的示例数据）
- 验证：`tests/panel-b.test.ts` 6 项；全量 38 文件 · 349 通过 + 5 预期失败；实拍 4 张（归因 / 记忆库 / 回收站 / 设置）
- 此时**三套面板并存**（默认 B + `?panel=old` + `?panel=sample`），直到 0.8.9 才收敛为单一实现

## 0.7.0 — 复刻档：样例 HTML 的 1:1 React 版（2026-09）

### 面板
- 把用户给的样例 HTML 做成 1:1 的 React 版，`?panel=sample` 切换，**并存不切默认**：「确认复刻档没问题后，我再把默认切过去、删旧组件」（docs/V0.7-SAMPLE-REPLICA.md:12）—— 切默认要等到 0.8.0
- DOM 结构、类名、层级、交互全部照样例写（`.modal` `.header` `.content` `.progress-section`）；数据全部接真：`/nexus/api/{memory,state,settings,models,neighbors}`，操作走同一批接口
- 只有 5 处偏离样例（都是物理约束或真实数据导致，已在 CSS 文件头备查），其中两处是真实库才暴露的问题：`.content > * { flex-shrink: 0 }`（样例只有 4 条数据看不出来，真实库 34 条时列表会把设置条压成 2px，实测）、`.list-container{max-height:52vh; overflow-y:auto}` + 一页 50 条 + 「加载更多」（不限制会让模态长到 3000px）
- 门禁：37 文件 / 343 通过 + 5 预期失败，typecheck 双档，`check:pack` 通过
- 复刻档缺的（当时如实列出）：注入明细（为什么没进入）、决策日志、作用域占比条（用进度条替代了）、项目选择器
- 同期的 v0.7 UI 专家团结论：7/7 一致「需要重构，但不必推倒」—— 数据层、路由、组件、token 全部可复用；病根是布局契约、客户端过滤、动作过载、反馈缺失、注入真相不可见

## 0.6.3 — 样例调色板双主题 + 780px 档逐项对齐（2026-09）

### 面板
- 新增 `scripts/style-diff.mjs`：把**样例 HTML** 和**面板**放进同一个 780px 宽的 iframe，同口径逐项量（此前两边尺寸不同，比出来的差异没有意义）
- 780px 档对齐：条目 padding 14px 16px ✅、卡片底色（亮色）#F8F9FA ✅；标题字号样例 13px vs 面板 16px（跟随宿主字号）⚠️ 差 3px —— 差值如实标注，不圆
- 宽度 780px vs 418px 是 ❌ 硬约束（面板不能自己变宽）；结论：独立页 `/nexus`（全宽）走 `.reference-scale` 档 = 全部对齐
- 样例调色板抽成双主题（亮 / 暗各一套）

## 0.6.2 — 独立页按样例比例、窄栏加全屏打开（2026-09）

### 面板
- 根因不是样式表是**宽度**：样例 HTML 是 720px 宽的独立弹窗，而 DSH 设置弹窗内容区只有 418px —— 在 418px 里照抄样例比例，只是把「能看到几条记忆」从 2 条变成 1 条
- 三步落地：独立页 `/nexus` 加 `.reference-scale`（本来就是全宽）；窄栏加「全屏打开」按钮（一键 `window.open('/nexus')`）；窄栏噪音横幅收细（94px → 约 40px），首屏多出一行记忆的位置
- 硬约束写进文档：面板宽度由宿主的设置弹窗决定，要真正 720px 只有两条路 —— 独立页 `/nexus`（全宽）或从窄栏「全屏打开」

## 0.6.1 — 照参考稿样例改（2026-09）

### 面板
- 用户明确「**就按照样例 HTML 改**」，本版按参考稿落地：占用卡 · 状态卡筛选 · 列表容器 · 两行条目 · 详情网格 · 冲突 Diff · 骨架屏
- **卡片即筛选入口**：状态卡点「冲突」直接切到该状态；展开三段式：原文块 → 冲突/重复对照（有才显示）→ 详情网格（ID / 作用域 / 类型 / 权重 / 置信度 / 占注入 / 更新时间）
- 绝对尺寸被实测否决：卡片 16px 内边距、14px 行内边距、`.list-container{min-height:200px}` 照抄进 418×448 后**首屏一条记忆都看不到** → 窄栏改紧凑档（8–10px / 7px / 无 min-height）
- 修掉 4 个布局坑，其中一个是根因级：flex 子项 `min-height:auto` 让 `.nx-listcard` 被内容撑到 542px、头部滚走 —— 修法 `min-height:0` + 列表 `overflow-y:auto`；定稿实测 `html/body/.nx-app = 448`、`docOverflow = 0`，外框不滚、只有列表滚
- 门禁：37 文件 / 333 通过 + 5 预期失败

## 0.6.0 — 面板外壳与交互增强（2026-09）

### 面板
- 面板外壳重做：**Grid 行展开动画**（`grid-template-rows: 0fr↔1fr`）+ 收起时 `inert`、常驻设置折叠条
- 交互增强（后来被参考稿沿用，记录在 docs/V0.6-PANEL-EVOLUTION.md §5）：右键上下文菜单（与行内「⋯」共用菜单项）· Shift 连选 / Cmd 加选 / ↑↓ 键盘焦点环 / 空格勾选 · 置信度圆点（`--nx-conf-high/mid/low`，非文本对比 ≥3:1）· 错误态「点击重试」+ Toast 撤销
- 打包命名从此版起改为 `chenqiuyushuang-dsh-nexus-<版本>.tgz`（此前是 `dsh-nexus-<版本>.tgz`；包名一直是 `@chenqiuyushuang/dsh-nexus`）
- 当时的基线：src 34 模块 / 20 个测试文件 / **193 测试通过 + 5 xfail** / 零运行时依赖；已知问题如实写着「面板产物偏大（700KB 内联 sourcemap）；无 CSP」

## 0.5.10 — 宽屏同屏三次重复同一组数字（2026-09）

- 上一版只修了窄栏：宽屏（独立页）上同一组数字仍在摘要行、注入条、统计胶囊三处重复出现
- 修复：摘要行与统计胶囊**彻底移除**（两种模式都不再渲染），它们独有的信息（跨项目/本项目分布、今日流量）移进展开区，顶部只留一条状态条

## 0.5.9 — 首屏不再是垃圾：默认只显示「在用」（2026-09）

- 默认筛选从「全部状态」改为**活跃**：此前一打开面板，最前面就是已归档垃圾（用户实拍「不好看」的主因之一）；状态随时可切回「全部状态」
- 顺带填补两处孤行：状态条第 2 行接上「决策日志」入口；「显示 N / 共 M 条」在窄栏并回工具栏第 1 行

## 0.5.8 — 顶部三层数字合并成一条状态条（2026-09）

- 用户反馈「不好看」：顶部叠了摘要行 + 注入条 + 统计胶囊共三层数字，数字比内容还占地方
- 修复：嵌入窄栏只留**一条状态条**（注入计数内联），摘要行与今日流量移进展开区；宽屏布局不变

## 0.5.7 — 展开一条长记忆 = 一堵文字墙（2026-09）

- 用户植入真实数据后反馈「展开一条长记忆后是一堵文字墙，而且不知道能不能收起」（docs/V0.7-PANEL-DESIGN.md:82）
- 修复：展开区限高 **96px + 内部滚动**（键盘可聚焦），折叠行加 `▸`、展开区加「收起 ▴」，手风琴（同时只展开一条）（同文件 :103-104）
- 根因与规矩写进 `docs/V0.7-PANEL-DESIGN.md §7`：此前的验证是**指标驱动**（行高 / 行距 / 首屏行数 / 是否溢出），从来不是**场景驱动** —— 指标永远不会告诉你「一堵墙很难看」；改 UI 前先枚举改动会落在场景矩阵哪几行，改完逐行看图再说「完成」；`panel:preview` 随之新增 `--expand-row`

## 0.5.6 — 安装摩擦修复：别再删掉用户 profile 正在引用的 tarball（2026-09）

- 用户 profile 里 pinned 的 `dsh-nexus-0.4.3.tgz` 在升版本时被删了 —— 后果是直接 `add` 新版会失败（pnpm 先解析旧依赖），必须额外走 `remove`
- 修复：从 git 历史重建该 tarball，让 profile 里那条 `file:` 依赖重新可解析；`npm run verify:install` 新增陷阱自诊断 —— 检查 profile 依赖指向的文件是否存在，不存在就明确提示「先 remove 再 add」
- 流程规则（此后遵守）：**保留用户 profile 当前引用的那个 tarball**，新版本另存新名，不删旧包

## 0.5.5 — README 重写，对齐 0.5.x 的真实能力与默认值（2026-09）

- 旧 README 有多个重复的安装章节、还在教 `github:` 安装、把待决策的向量检索当卖点、并引用了已删除的 `web/nexus.html`
- 新版：三条安装路径（本机 tarball / GitHub / npm）+ **两个已踩过的坑**（版本号不变 pnpm 不更新、插件代码与面板在 `dsh web` 启动时读入内存）+ 校验命令
- 新增「你会得到什么」（注入真相 / 预算可见 / 可信度闭环 / 一键清理 / 三态删除）、命令清单、成本契约表（逐条核对默认值）、开发与工具清单、已知限制（诚实清单）；文档索引补齐 V0.7 / V0.8 / V0.9

## 0.5.4 — 压缩感知重注入：记忆不再静默消失（2026-09）

- 问题：注入策略是「首轮一次 + 内容指纹变化才刷新」，而 DSH 的上下文压缩会把已注入的记忆块移出窗口 —— 指纹没变、又不是首轮，于是记忆再也不会出现（用户以为它坏了）
- 依据：DSH 会发 `compaction/start|end|prune|summary` 四种会话事件，取「结束」与「裁剪」两种（`src/scheduler.ts`）；压缩/裁剪后清掉该会话的注入标记，下一次 `agent/pre-step` 重新注入（走同一条记账路径，受每日预算与 1KB 上限约束）
- 已知代价：若旧注入块在压缩后仍残留，可能出现一份重复（≤1KB）；比「记忆静默消失」可接受

## 0.5.3 — 包体门禁 + 删掉「第二套面板」（2026-09）

- 新增 `scripts/check-pack.mjs`（`npm run check:pack`，接入 CI）：白名单校验（不泄露 src/tests/scripts/截图）、必需文件齐全、**包内 `lib/*` 与工作区构建逐字节一致**（抓「忘了重新构建」）、体积上限（512 KB）、无构建中间产物
- 门禁上线即抓到：包里带着 `web/nexus.html` —— 手写的**第二套面板**（另一套 CSS/JS，161 行），构建脚本并不使用它，只在产物缺失时兜底；后果是「万一 lib 缺失就渲染另一套界面」。已删除并强制单一实现
- 此后包内只有 7 个文件（`package.json.files` 只列 4 个产物），产物缺失时返回诚实的内联说明页

## 0.5.2 — `/memory doctor` 自检（2026-09）

- 动机：用户的第一个问题不是功能不够，而是**不知道它有没有在工作**
- 新增 `src/doctor.ts`（纯函数 `diagnose`）+ `/memory doctor`：一次给出库计数、待裁决冲突、注入占用与未进入数、LLM 提炼开关、降级状态、价值门影子计数，并给一句结论
- 分级：不可写 = ✖ bad；垃圾 / 冲突 / 零活跃 / 零注入 / 降级 = ⚠ warn；其余 · info；附 `scripts/doctor-preview.mjs` 用线上真实库数据预演输出

## 0.5.1 — 功能面审计：删真·死字段，把「砍功能」交回决策（2026-09）

- `atom.embedding` 是真死字段（无读无写、磁盘无记录、schema 非 strict）→ 已删
- `vector` / `integrator` / `skill-compiler` 都**仍可触发**（配置项或 `/memory` 命令），属于「砍功能」而非清死代码 → 不擅自删，影响面写进 `docs/V0.9-SURFACE-AUDIT.md` 待决策（三个模块合计 288 行、零测试覆盖）
- 证据：`~/.dsh/nexus/` 下无 `skills.draft/`（skill-compile 从未运行）、配置里无 vector / integrator

## 0.5.0 — V0.5 可信度闭环：今天到底写进了几条真东西（2026-09）

- 动机：系统只有「总量」没有「今日流量」—— 用户无法判断它今天是在学习还是在污染；清理一次垃圾建立不了信任，连续几天看到「写入少、拒收有、注入有」才是可信信号
- 新增 `src/today.ts`（纯函数）：按**本地日历日**统计写入（排除当天归档/取代）/ 待确认 / 拒收（区分规则拒收与用户拒收）/ 注入次数与字节
- `/state.today` 输出计数 + 一行人话；面板把它并进已有摘要行，不额外占高度
- 同期的 v2 设计定稿把取舍写死：写入双通道（即时规则 0 token / 会后 LLM 按预算）、注入混合模型（首轮 ≤1KB 索引 + 增量 + 按需检索）、生命周期墓碑化不硬删、安全本地优先、质量门 golden 集先行（docs/V0.5-DESIGN.md D1–D6）

<!-- 0.4.2–0.4.9 未单列：这几个版本的改动已写在下方 0.4.1 条目里（按要求保持原样不动），此处不重复记载 -->

## 0.4.1 — 面板可用性与写入纯度（2026-09）

### B1 布局契约
- 嵌入态（设置面板 iframe）改为固定高 flex 列，只有列表滚动：头部/统计/工具条不再滚出视野；高度回传上限 620px（宿主可用高度 −24）

### B4 注入真相（面板不再说假话）
- `/nexus/api/state` 新增 `injection` 真相与 `project/projects`：按运行时同一套规则复算（forContext 严格隔离 + buildIndex 字节预算），逐条给出「进不进、为什么」
- 六种原因分组：单条超预算 / 被挤掉 / 归属未知 / 属于其他项目 / 会话记忆 / 非活跃；顶部注入条 + 折叠后的分组清单，每组一键动作（缩短 · 置顶插队 · 指派项目 · 改为用户级 · 确认）
- 修复：面板此前用 buildIndex(全部 active) 统计 → 其他项目与归属未知（unknown）的记忆被算成「已注入」，实际永远不会进上下文（实测 11 条）
- `/nexus/api/memory/update` 支持 `projectRef`（指派或清除项目归属）
- 测试：`injection-truth` 单元 5 例 + 路由级 2 例 + 面板渲染 2 例；全套 222 通过 + 5 xfail

### 宽屏同屏三次重复同一组数字（自查发现）
- 现象：宽屏（独立页）上「在用 4」同时出现在摘要行、状态条、统计胶囊三处；「注入 603 B」出现两处 —— 我上一轮只修了窄栏，宽屏原样保留
- 修复：摘要行与统计胶囊**彻底移除**（两种模式都不再渲染）；它们的独有信息（跨项目/本项目分布、今日流量）通过 `detail` 进状态条展开区
- 实测：宽屏（1080）首屏 7 → **9 行**、顶部从 3 行数字降到 **1 行**；窄栏 418×448 保持 2 行状态条 + 2 行工具栏
- 顺带修掉一处 `{false && …}` 关代码导致 typecheck 报错（已删除该死代码块）
- 测试 316 通过 + 5 xfail；包体门禁通过
### 首屏不再是垃圾：默认只显示「在用」+ 工具栏与状态条补空位
- **默认筛选改为「活跃」**：此前默认「全部状态」，一打开就是 27 条已归档垃圾排在最前（用户实拍「不好看」的主因之一）；现在首屏就是那 4 条真记忆，状态可随时切回「全部状态」
- **填补两处孤行**：状态条第 2 行原只有「在用 4 · 待确认 0 · 冲突 2」，现接上「决策日志」入口；工具栏原第 3 行只有「显示 33 / 共 33 条」，窄栏改为「33 条」后并回第 1 行
- 实测（418×448 暗色）：顶部数字 5 行 → 2 行；工具栏 3 行 → 2 行；面板高度 546 → **433**；首屏 4 条真记忆（无垃圾）
- 372×460 档：工具栏 3 行、列表 4 行；宽屏无回归（行高 41 / 首屏 7+ 行）
- 测试 316 通过 + 5 xfail
### 顶部三层数字合并成一条状态条（用户：「不好看」）
- 问题：顶部叠了「摘要行（2 行）+ 注入条（2 行）+ 统计胶囊（1 行）」共 **5 行数字约 110px**，而记忆只有 4 条 —— 数字比内容还占地方
- 修复：嵌入窄栏只留**一条状态条**（注入条内联计数），摘要行与今日流量移进展开区；宽屏布局不变
- 实测（418×448 暗色）：面板高度 546 → **458**；列表 4 行 → **5 行**；顶部数字行 5 → 2
- 372×460 档同样 5 行可见（工具栏 4 行，未回归）
- 宽屏无回归：行高 41 / 行距 47 / 首屏 7+ 行
- 测试 316 通过 + 5 xfail
### 展开一条长记忆 = 一堵文字墙（用户实拍指出）
- 现象：在真实弹窗里展开一条 1191 字的记忆，得到 10 行文字墙，把整个列表视口推走；且折叠行没有任何「可展开」暗示，用户只能问「能收起吗」
- 根因：此前的 UI 验证是**指标驱动**（行高/首屏行数/溢出），从不是**场景驱动** —— 指标不会告诉你「难看」；工具早就有，缺的是状态矩阵
- 修复：展开区限高 **96px（≈5 行）+ 内部滚动**（键盘可聚焦，WCAG 2.1.1）· 折叠行加 `▸` · 展开区加「收起 ▴」· **手风琴**（同时只展开一条）· 截断提示缩短一行
- 场景矩阵与规则写进 `docs/V0.7-PANEL-DESIGN.md §7`：改 UI 前先枚举会落在矩阵哪几行，改完逐行看图再说「完成」
- 工具：`panel:preview` 新增 `--expand-row`（展开第一行的真实场景）
- 测试 316 通过 + 5 xfail
### 安装摩擦修复：别再删掉用户 profile 正在引用的 tarball
- 现象：用户仍停在 0.4.3，而他的 profile 依赖指向 `dsh-nexus-0.4.3.tgz` —— 那个文件在升版本时被我删了。后果：**他直接 `add` 新版会失败**（pnpm 先解析旧依赖），必须额外走 `remove`。一个会话发 12 个版本、每次删旧包，是我把安装流程变复杂的
- 修复 1：从 git 历史（`d602a0d`）重建 `dsh-nexus-0.4.3.tgz`，让 profile 里那条依赖重新可解析 → 用户更新只需 `add`，不必 `remove`
- 修复 2：`npm run verify:install` 新增陷阱自诊断 —— 检查 profile 的 `file:` 依赖指向的文件是否存在，不存在就明确提示「先 remove 再 add」
- 流程规则（今后遵守）：**保留用户 profile 当前引用的那个 tarball**，新版本另存新名，不删旧包
### README 重写（对齐 0.5.x 的真实能力与默认值）
- 旧 README 有多个重复的安装章节、还在教 `github:` 安装、把待决策的向量检索当卖点、并引用了已删除的 `web/nexus.html`
- 新版：三条安装路径（本机 tarball / GitHub / npm）+ **两个已踩过的坑**（版本号不变 pnpm 不更新、插件代码与面板在启动时读入内存）+ 校验命令
- 新增「你会得到什么」（注入真相 / 预算可见 / 可信度闭环 / 一键清理 / 三态删除）、命令清单、成本契约表（全部核对过默认值）、开发与工具清单、已知限制（诚实清单）
- 文档索引补齐 V0.7/V0.8/V0.9 四份
### 压缩感知重注入（P1 收尾）
- 问题：注入策略是「首轮一次 + 内容指纹变化才刷新」，而 **DSH 的上下文压缩会把已注入的记忆块移出窗口** —— 指纹没变、又不是首轮，于是记忆再也不会出现（用户以为它坏了）
- 依据：DSH 会发 `compaction/start|end|prune|summary` 四种会话事件（known-event-types），取 `end` 与 `prune`
- 修复：压缩/裁剪后清掉该会话的注入标记，下一次 `agent/pre-step` 重新注入（走同一条记账路径，受每日预算与 1KB 上限约束）
- 已知代价：若旧注入块在压缩后仍残留，可能出现一份重复（≤1KB）；比「记忆静默消失」可接受
- 测试：scheduler 新增 1 例（end/prune 触发、start/summary 与普通消息不触发）；全套 316 通过 + 5 xfail
### 包体门禁 + 删掉「第二套面板」
- 新增 `scripts/check-pack.mjs`（`npm run check:pack`，已接入 CI）：白名单校验（不泄露 src/tests/scripts/截图）、必需文件齐全、**包内 lib/* 与工作区构建逐字节一致**（抓「忘了重新构建」）、体积上限、无构建中间产物
- 门禁上线即抓到：包里带着 `web/nexus.html` —— 那是 9月8日手写的**第二套面板**（11KB，另一套 CSS/JS），构建脚本并不使用它，只在产物缺失时兜底。后果是「万一 lib 缺失就渲染六天前的界面」。已删除并强制单一实现（产物缺失时返回诚实的内联说明页）
- 包体：8 个文件 → **7 个**，120 KB（上限 512 KB）
- 测试 315 通过 + 5 xfail
### `/memory doctor` 自检（回答「它到底在不在正常工作」）
- 动机：专家团（可解释性/产品）反复指出，用户的第一个问题不是功能不够，而是**不知道它有没有在工作**
- 新增 `src/doctor.ts`（纯函数 `diagnose`）+ `/memory doctor`：一次给出库计数、待裁决冲突、注入占用与未进入数、LLM 提炼开关、降级状态、价值门影子计数，并给一句结论
- 分级：不可写 = ✖ bad；垃圾/冲突/零活跃/零注入/降级 = ⚠ warn；其余 · info
- 测试：doctor 4 例（正常/不可写/多项异常/影子计数仅在有时显示）；全套 315 通过 + 5 xfail
- 附 `scripts/doctor-preview.mjs`：用线上真实库数据预演 doctor 输出
### 功能面审计 + 删除真·死字段
- 核对专家团「删除清单」的真实影响面：`atom.embedding` 是真死字段（无读写、磁盘无记录、schema 非 strict）→ 已删；`vector`/`integrator`/`skill-compiler` 都**仍可触发**（配置项或 `/memory` 命令），属于「砍功能」而非清死代码 → 影响面写入 docs/V0.9-SURFACE-AUDIT.md 待决策
- 证据：`~/.dsh/nexus/` 无 `skills.draft/`（skill-compile 从未运行）、配置里无 vector/integrator、三个模块合计 288 行零测试覆盖
- 测试 311 通过 + 5 xfail
### V0.5 可信度闭环：今天到底写进了几条真东西
- 动机：产品专家指出系统只有「总量」没有「今日流量」——用户无法判断它今天是在学习还是在污染；清理一次垃圾建立不了信任，连续几天看到「写入少、拒收有、注入有」才是可信信号
- 新增 `src/today.ts`（纯函数）：按**本地日历日**统计写入（排除当天归档）/待确认/拒收（区分规则拒收与用户拒收）/注入次数与字节
- `/state.today` 输出计数 + 一行人话；面板把它并进已有的摘要行（不额外占高度）：
  「在用 4 条（跨项目 1 · 本项目 3）· 注入 691 B / 1024 B · 今日写入 2 条 · 待确认 1 · 拒收 3 条 · 注入 5 次 / 2780 B」
- 测试：today 3 例（日历日边界、区分拒收来源、零流量不显示假进度）+ /state 路由断言；全套 311 通过 + 5 xfail
- 工具：`scripts/panel-preview.mjs` 重写并新增 `--stub-state`（把 today/noise/valueGateShadow 注入每个文档），
  这样「运行中的服务还是旧代码」时也能在本地验证新字段的渲染
### 预算挤占提示（吃掉 30% 以上预算的记忆标黄 + 就地缩短）
- 动机：实测你库里一条项目记忆 **591 B / 1024 B（58%）**，一条就吃掉了大半注入预算
- 注入真相：「已进入」按字节降序，单条 ≥30% 预算标黄，并给一句可执行提示；**已进入的行也提供「缩短」**（改完自己看一眼截图才发现：提示指向的按钮当时根本不存在）
- 列表行展开态显示「占注入 N B（预算的 X%）」，≥30% 标黄
- 测试：panel-injection 增加标黄断言（12% 不该标）；全套 308 通过 + 5 xfail
### 「记忆＝预算」可见 + 注入行去重（看图发现的真问题）
- **注入行主语重复**：抽取器把 subject 设为 statement 前 24 字，于是短记忆渲染成「用户的名字是 Daniel(中文对话)。：用户的名字是 Daniel(中文对话)。」——用户每天在自己的系统提示里看到这种重复，还白占预算字节。修复：主语是正文前缀时只打印一次（`renderIndexLine` + 注入条行 + 投影文件同规则）
- **每条记忆的预算成本可见**：`/memory` 列表项新增 `injectBytes`（与运行时 `renderIndexLine` 同源，有测试断言两者相等）；行展开态显示「占注入 N B」
- **「谁在吃预算」一眼可见**：注入真相的「已进入」按字节降序，每条显示「N B · X%」，汇总行显示「已用 / 预算（占比）· 剩余 N B」
- 测试：新增 `tests/index-line.test.ts` 4 例（去重、前缀、非前缀不丢信息、字节更省）；全套 308 通过 + 5 xfail
### V0.9 价值密度门（影子期）—— 写入判别
- 背景：专家团与实测一致指向「写入判别缺位」——33 条里 29 条是垃圾，命中率 12%，唯一质量信号是触发词「记住」
- 新增 `src/value-gate.ts`：`assessValue` 给每条候选打 0–100 分并给理由（身份/偏好 +30、约定/规范 +20、路径/技术标识 +15、长度合适 +15；子代理回执与提示词 −100 一票否决、元讨论 −40、问句收尾 −30、截断片段 −25）
- **影子回放（真实库 33 条）**：29 条垃圾 100% 拦下，2 条真记忆保住（名字/尽调项目），2 条用户自己说「不该记」的测试句拦下
- 影子期只记录不拦截：`/state.valueGateShadow` 累计 accept/review/reject，面板决策日志里显示；切换拦截的条件写进 docs/V0.9-VALUE-GATE.md（连续 7 天、reject ≥50 条、误拦率 0）
- 新增 `scripts/value-gate-replay.mjs`：对线上库跑回放，输出混淆矩阵与拦下原因分布
- 测试：value-gate 4 例（含真实样本回放与一票否决）；全套 291 通过 + 5 xfail
### 缩放体检（无障碍专家提出，实测后只改一半）
- 专家担心：宿主 17px 或浏览器 125–150% 时，固定高 flex 列会顶破 620px 上限、头部随外层滚走（B1 复发）
- **实测结论：不会复发**。听话宿主下 14/17/20px 三档 `docOverflow = 0`、头部恒在 y=28；即使宿主拒绝长高，14px 列表 4 行、17/20px 列表 3 行且无溢出
- **但专家说对了另一半**：窄栏里控件字号是硬编码 px（12px / 11.5px），正文跟随宿主而控件不跟。已改为 `0.86em` 相对单位（14px 下仍是 ≈12px，布局零变化；17/20px 下控件随之变大，工具栏 3→4 行，列表仍 6 行）
- 内边距保持 px：这是密度取舍，已写进本节；测试 287 通过 + 5 xfail
### 清理后的两个「看着像没生效」问题（用户截图指出）
- **「未进入 31」在清理后仍是 31**：`dropped` 把已归档/已取代的条目也算进去了。改为：归档/取代类彻底出局，单列 `archived` 计数；只有「待确认/冲突」这类可操作项才计入「未进入」。徽标拆成「未进入 N」+「已归档 M」，展开区加一句说明
- **一键清理后列表仍显示全部状态**：27 条归档垃圾还在眼前 → 清理成功后自动把状态筛选切到「活跃」并在 toast 里说明
- 测试：injection-truth 新增「归档不计入未进入」1 例、panel-injection 新增「已归档 M」徽标断言；全套 288 通过 + 5 xfail
### 窄栏可读性（设置弹窗里的日常入口）
- 搜索框在窄栏收成放大镜（46px），聚焦时展开 —— 工具栏 4 行 → 3 行 → **2 行**
- 折叠行不再显示「活跃」（无信息量且占掉约 1/4 正文宽度）：加状态类，只隐藏 `status-active`，**待确认/冲突/已归档照常显示**
- 实测（418×448 暗色）：正文可用宽度 **264px**，列表 6 行，面板申请高度 553
### 嵌入态高度协商：面板只能请求「当前高度」的自我锁定
- **根因**：`.nx-app` 是 `height: 100%`，而上报用的是 `documentElement.scrollHeight` → 恒等于当前 iframe 高度 → 面板永远只能申请「维持现状」（实测上报 448 = 当前高度），列表再挤也不会变高
- **修复**：按内容算期望高度 = 列表以外各区块的实测高度 + 列表期望高度（最多 6 行、最少 160px），上限 620（宿主告知可用高度时取 hostHeight−24）
- **客户端（设置项）**：补上 `nexus-viewport` 上报宿主可用高度（0.5s 轮询 + resize 监听），面板才知道自己能占多高
- 实测（418×448 暗色，复刻设置弹窗）：面板申请高度 448 → **562**；列表 177 → **291px**；可见行 3 → **6**
### 嵌入窄栏布局实测修复（用户截图指出的不合理）
- **根因**：`.nx-app` 是 flex 列，但只有列表设了 `flex: 1`，其余子项默认 `flex-shrink: 1` 被压缩；注入条带 `overflow: hidden` → 自动最小高度为 0 → **在设置弹窗的 372px 窄栏里被压成 6px 整条消失**（不是审美问题，是核心信息消失）。修复：`html.embedded .nx-app > * { flex: none }` + 列表 `min-height: 160px`
- 页脚（63px，比列表还高）在嵌入态隐藏 → 空间还给列表
- 工具栏在窄栏从 4 行 108px 收到 3 行 79px；刷新与全选本页收进「更多」占位（列表每 15s 自动刷新，全选仍可点勾选框）
- 注入条在窄栏只留数字（隐藏进度条与「进入上下文」标签）→ 单行 43px，不再换行
- 统计与占比条压矮一档（33→27 / 32→26）
- **实测（372x460，即 DSH 设置弹窗里的真实尺寸）**：列表 66px/1 行 → **186px/4 行**；注入条 6px → 43px；页脚 63px → 0
- 新增 `npm run panel:preview`（scripts/panel-preview.mjs）：用 CDP Fetch 拦截把 /nexus 换成当前本地构建，在指定尺寸的 iframe 里量布局并截图，专门用于复刻「设置弹窗」场景
### P0 正确性：存进库的话必须和用户说的一致
- **`stripTrigger` 全局删触发词**：旧实现用 `/g` 替换，句中出现「记住/别忘/我一直用」也会被删 → 存成与用户原话不同的句子（比疑问句误记更隐蔽）
- 修复：只循环剥离**句首**的脚手架 + 触发词链（「记住，我们约定如下：X」→「X」，保持既有断言），句中的触发词一律保留；再加护栏——剥完若只剩裸词（无中文且 <3 词）就不剥，避免「记住：我一直用 pnpm」变成「pnpm」
- 测试：extraction 新增 2 组案例（句首链剥净 / 句中的记住·别忘记·我一直用必须保留）；全套 286 通过 + 5 xfail
### P0 污染根因（「33 条里只有 2 条是真的」）
- **根因 1：DSH 把子代理/后台任务回执当 user 消息注入父会话**，Nexus 当成用户说的话记下来 —— 实测 22 条噪音的来源会话全是同一个父会话。修复：`session/event` 的 user 分支在入库前丢弃系统通知（`isSystemNotificationText`）
- **根因 2：长文提示词被写成记忆** —— 6 条 2KB 专家提示词（`sources: []`、`user-declared`），来自面板「新增」或模型工具。修复：模型工具 `memory_remember` 加写入门控（≤600 字 + 拒绝提示词/文档结构），并把门控抽成可单测的 `memoryWriteRejection`
- **检测升级**：`src/noise.ts` 现在统一判定「疑似无效记忆」= 子代理回执 + 提示词/文档长文（`## 背景/任务/输出格式` + `你是**`），面板横幅文案随之改为「疑似无效记忆」+ 一键清理
- **线上实测**：33 条中命中 29 条（22 回执 + 7 文档），清理后剩 4 条（名字 / 尽调报告项目 / 2 条用户测试句）
- 测试：noise 7 例（含负例：中文长笔记、提到 UUID 的正常句不误伤）
### P0 实测修复（「有没有真的看过面板」之后）
- **「⋯」按钮被同名 CSS 撑高**：`.nx-more` 同时被「加载更多」容器与行内按钮使用，后者被前者的 `padding: 10px 0 4px; display:flex` 覆盖 → 按钮 39px、整行 57px。容器改名 `.nx-loadmore`，按钮改用 `font: inherit` 之后设字号/行高 → 行高 **57 → 41px**，行距 63 → 47px
- **列表上方吃掉 337px**：压缩标题/占比条/统计/工具条的间距与内边距；决策日志按钮移进工具条；嵌入态隐藏副标题 → 列表起点 **337 → 273px**（700px 视口下首屏约 9 行，达标 ≥8）
- **「确认」按钮竖排**：`.nx-btn` 缺 `white-space: nowrap; flex: none`，被 flex 压缩成两字宽；已修
- **子代理噪音有了治理入口**：新增 `src/noise.ts` 保守识别（模板句 + 会话 UUID 同时命中；含 5 条负例防误伤）、`/state` 暴露 `noise{count,ids}`、面板顶部横幅 + 二次确认「一键清理」（归档 + 黑名单，5 秒可撤销）。本机实测 33 条中 **22 条（67%）是子代理噪音，其中 21 条是 user 作用域**
- **新增实测工具 `npm run panel`**（`scripts/panel-shot.mjs`）：无头 Chrome 打开已安装面板 → 注入本地构建 → 输出各区块位置/高度、行高/行距、首屏行数、异常按钮，并截图到 `_shots/`。UI 改动从此有「真的看一眼」的固定动作
- 测试：noise 3 例（含负例）；全套 280 通过 + 5 xfail
### B7 视觉与主题
- 颜色只给状态：槽位/资料标签统一中性底，作用域改由行首色条表达（折叠与展开都在）
- 新增作用域占比条（`/state` 的 `byScope`）：分段进度 + 图例，`role="img"` + 整句 aria-label；占比用最大余数法，三项相加恒为 100%
- 暗色标签不再「消失」：新增 `--nx-tag-bg`（亮色 bluish-100 / 暗色 `rgba(255,255,255,.10)`），并加 ΔL* ≥ 3 的算术测试（原来 --nx-bg-accent 与行底同为 bluish-800）
- 主题与字号跟随宿主：嵌入态读宿主的 `data-ds-dark-theme`（MutationObserver 同步切换）与计算字号；`?fontSize=` 可覆盖，越界夹到 12–17，跨域回退 14
- 验收：折叠行红色按钮 0（标准 ≤1）；行高预算 38px + 行距 6px → 一屏约 10 条（标准 ≥8）
- 测试：theme 3 例 + scopebar 4 例 + 对比度新增 2 例（ΔL*）；全套 277 通过 + 5 xfail
### B6 可访问性
- 键盘：`/` 聚焦搜索（输入框内不抢键）；「⋯」菜单打开即聚焦首项、↑↓ 环绕、Home/End、Esc 关闭并把焦点还给按钮；下拉筛选同样支持方向键/Esc/回焦
- 语义：列表 `role="list"` / 行 `role="listitem"`；结果计数为 `role="status" aria-live="polite"`（toast 已是 polite 播报）
- 对比度：亮色下三档文字全部 ≥4.5:1（原 secondary 5.80 / tertiary 3.71 / muted 2.73），warn-text → amber-700、error-text → red-700，新增 `--nx-accent-text`（原 accent 白底 4.23）；暗色 warn-text → amber-400、error-text → red-300
- `prefers-reduced-motion: reduce` 下关闭过渡与动画
- 测试：对比度算术 30 例（解析 theme.css 的 var() 链，亮暗各 15 组）+ 键盘纯逻辑 4 例 + jsdom 焦点交互 4 例 + 可访问名审计 2 例；devDependency 新增 jsdom（仅测试用）
- 全套 268 通过 + 5 xfail
### B3 列表重做（密度）
- 行拆成 `src/ui/MemoryRow.tsx`：折叠态 = **单行**（勾选框 + 作用域色条 + 正文省略号 + 状态字 + ⋯，约 38px，行距 6px → 一屏约 10 条）
- 展开后才显示：标签行、完整正文、元信息、冲突与疑似重复提示；键盘 Enter/Space 也可展开收起
- 设计文档 §6 记录行高预算修正：原「标签行 + 两行正文」约 72px，一屏仅 6 条，与「首屏 ≥8 条」自相矛盾
- 次要操作收进「⋯」菜单（编辑/置顶/合并重复/关系/归档/移入回收站/恢复/彻底清除），危险操作在菜单内二次确认
- 新增批量选择：勾选后出现批量条（确认 / 归档 / 移入回收站，均 5 秒内可撤销）+「全选本页」；不做批量置顶（会一次性挤爆 1KB 注入预算）
- `content-visibility: auto` 轻量虚拟化；单页 80 → 50 条（DOM 行 ≤60）
- 空态改为可操作引导（会话里说「记住：…」或点「新增」）
- 测试：`panel-list` 渲染 4 例（折叠态无元信息/展开才有/待确认主操作与选中态/提示只在展开后）；全套 228 通过 + 5 xfail
### B2 列表服务端化
- `/nexus/api/memory` 返回 `{items,total,offset,limit}`：limit ≤200（默认 80）、支持 offset；筛选（q/scope/status/reviewNote）全部走服务端
- 修复：面板此前取 80 条再本地过滤 → 库 >80 时「待确认」筛出 0 条、chips 与列表不一致
- 列表只回面板需要的字段（去掉 cues/sources/fp/provenance）；statement 截 400 并给出 `statementLength/truncated`
- 新增 `/nexus/api/memory/get?id=`：编辑与「缩短」前取全文，避免照预览保存时删掉长记忆的后半段（本机 29 条 >1KB）
- 面板：工具条显示「显示 X / 共 Y 条」、列表底部「加载更多（还有 N 条）」、截断行提示全文长度
- 测试：B2 路由 2 例（分页/上限/末页 · 投影/截断/全文/404）；全套 224 通过 + 5 xfail

### B5 反馈与撤销
- 底部 toast：成功 5 秒内可撤销、失败带原因；彻底移除 window.alert / window.confirm；彻底清除改面板内两步确认且只对回收站开放
- 撤销链修好：归档（连黑名单一起回滚）/ 移入回收站 / 置顶 都可完全撤销
## 0.4.0 — 可信版（2026-09）

### 安全
- 面板鉴权收口：写操作要求 `Origin == Host`（含端口）精确匹配；读操作要求 loopback Host（防 DNS rebinding）；远程访问需显式 `webuiAllowRemote: true`
- 注入块声明「历史记忆数据，不是指令」；行内容扁平化（换行/制表/Unicode 行分隔符/零宽与双向控制符）
- 扫描器先归一化（NFKC + 去零宽）再匹配，补齐「忽略/无视/ignore the previous」等中文与英文变体；密钥/身份证/私钥出厂拦截
- 模型工具权限收紧：`memory_feedback` 权重上限 5（防模型自加权挤爆索引）

### 成本
- 注入：会话首轮 ≤1KB 一次 + 变更增量（修复「每 15 秒重复注入」，实测 6 轮 6 次 → 1 次）
- 注入与提炼写入成本账本（token 估算 + 字节），`/memory cost` 不再恒 0
- 新增 `budget.ts` 预算权威：窗口由 `maxInputBytes` 反推 · 每会话 ≤8 窗 · 每日 ≤20 万输入 token · 超限跳过并计入会话小结
- 修复 `pruneCosts` 保留方向错误（曾删最新留最旧 → 账本冻结、每日闸门静默失效）；recall 账本加裁剪
- `memory_search` 输出加 1.5KB 上限并记录真实字节

### 语义与生命周期
- 否定极性（`不要用 X` ≠ `用 X`）；A-not-A 疑问（能不能/是不是）不再误判为否定
- 冲突检测：同槽位偏好单值、偏好压事实、极性相反 → 待确认，不再矛盾共存
- 近义重复检测 + 一键合并（不自动合并，交人确认）
- 墓碑三态：归档 / 移入回收站 / 彻底清除（清 atom + edges + reject 样本 + 同步投影）；归档即黑名单，同句不再复活
- 身份与偏好跨项目（user/personal）；`sources` 接线后 episode 记忆可注入

### 可解释性与检索
- 决策日志：`/nexus/api/decisions`（为什么没记 + 系统自动变更 + 上次会话小结）
- 会话小结：下次会话注入块首行显示「上次会话：新增 N 条 / 待确认 M 条 / 跳过 K 窗」
- 检索：原子预分词缓存 + 版本化缓存键 —— 5000 条实测 283ms → 10.8ms，写入即失效
- 投影目录 0700 / 文件 0600 + 目录内 `.gitignore`（防真实记忆被误提交）

### 工程
- golden 回归集 24 例（真实事故 + 泛化 + 负例）+ 5 条 xfail 显式跟踪已知缺陷 + GitHub CI
- 测试：190+ 通过；依赖对齐 DSH `0.1.5-rc.1`
